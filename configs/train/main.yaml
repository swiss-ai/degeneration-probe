defaults:
  - model: qwen05b
  - probe: linear
  - _self_

# Data
train_data: tests/fixtures/sample_data.jsonl
eval_data: null
eval_fraction: 0.2
max_length: 2048

# Training
learning_rate: 1e-3
batch_size: 4
num_epochs: 10
seed: 42
pos_weight: null

# Logging
wandb_project: null
wandb_run_name: null

# Output
output_dir: outputs/probes
